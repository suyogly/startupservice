import coreWebVitals from "eslint-config-next/core-web-vitals";

/** Native flat config (Next 16+); `next lint` was removed from the CLI. */
const eslintConfig = [...coreWebVitals];

export default eslintConfig;
